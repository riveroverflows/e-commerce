## Pull Request (PR) 가이드

과제는 GitHub Pull Request(PR) 기반으로 제출합니다. 아래 가이드를 참고하여, 깔끔하고 명확한 PR을 작성해주세요.

---

### PR 작성 절차

1. **기능 단위로 브랜치 생성**
    - 예시: `feat/week3-order-cancel`, `fix/week5-coupon-bugfix`
    - 브랜치는 항상 `riveroverflows` 기준으로 생성합니다.
        ```bash
        git switch riveroverflows
        git pull origin riveroverflows
        git switch -c feat/week1-user-tests
        ```

2. **커밋 작성**
    - 커밋 메세지는 명확한 동사 + 작업 대상 구조로 작성합니다.
    - 커밋 메세지만으로도 어떤 작업에 해당하는 내용인지 알 수 있도록 고민해보세요.
    - 예시:
        - `feat: 주문 생성 기능 구현`
        - `refactor: 장바구니 엔티티 리팩토링`
        - `test: 주문 생성 테스트 코드 추가`

3. **PR 생성**
    - 브랜치를 GitHub에 Push 한 뒤, PR을 생성합니다.
    - PR 제목은 아래 규칙을 따릅니다:
        ```text
        [1주차] 작업 내용 요약
        예: [1주차] 회원가입, 로그인 구현
        ```

4. **PR 템플릿을 채워서 제출**
    - 반드시 `💬 리뷰 포인트` 항목을 작성해주세요.
        - 멘토가 중점적으로 봐주었으면 하는 내용을 3개 이내로 작성해주세요.
        - 질문이 구체적일수록, 원하는 답변을 얻을 수 있습니다.

---

### PR 작성 시 유의사항

| 항목         | 설명                                                                           |
|------------|------------------------------------------------------------------------------|
| 리뷰 포인트 필수  | PR 템플릿 내 `💬 리뷰 포인트`는 반드시 채워야 합니다.                                           |
| 불필요한 코드 제거 | 테스트 코드, 디버깅 로그(`System.out`, `println`, 주석), 사용하지 않는 클래스/메서드 등은 PR에서 제거해주세요. |
| 테스트 커버리지   | 기능을 구현했다면 가능한 단위 테스트 또는 통합 테스트 코드를 포함해주세요.                                   |
| 코드 스타일     | 기본적인 포맷팅, 컨벤션을 지켜주세요. (IDE 자동 정리 활용)                                         |

---

### PR 템플릿

> 실제 템플릿: `.github/pull_request_template.md` 참조